name: Build Android APK

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'                    # ← bumped from 20

      - name: Generate Android Project (using npx)
        run: |
          npx @pwabuilder/cli generate \
            --url "https://emmaflames125-alt.github.io/Compiler-port/" \
            --platform android \
            --output ./android-app \
            --package-id "com.emmaflames125alt.fxespluscompiler" \
            --app-name "fxES+ ROP Compiler" \
            --short-name "fxES+"

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Build APK
        working-directory: ./android-app
        run: |
          chmod +x gradlew
          ./gradlew assembleRelease --stacktrace

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: fxesplus-apk
          path: ./android-app/app/build/outputs/apk/release/*.apk
